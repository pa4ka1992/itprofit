export const newsCount = 15;
export const dateResLength = 10;
export const errorClass = 'error';
export const errorLifeTime = 2000;
export const startHeader = 'top headlines';

export enum Queries {
  container = 'main .container',
  photo = '.meta-photo',
  author = '.meta-author',
  date = '.meta-date',
  title = '.description-title',
  source = '.description-source',
  cont = '.description-content',
  read = '.read-more a',
  news = '.news',
  item = '.item',
  temp = '#newsTemp',
  topic = '.topic',
}
